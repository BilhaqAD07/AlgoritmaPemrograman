#include <iostream>
#include <string>

using namespace std;
int main()
{
   string raptor_prompt_variable_zzyz;
   ?? int;
   ?? float;
   ?? k;
   ?? bil;
   ?? w;
   ??[] ting = new ??[??+1];

   int =bil;
   float =ting(5);
   while (!(k<5))
   {
   }
   cout << "Data ke- =" << endl;   raptor_prompt_variable_zzyz =float;
   cout << raptor_prompt_variable_zzyz << endl;
   cin >> ting(k);
   cout << "------PENCARIAN DATA-----" << endl;   cout << "Nilai yang akan dicari = " << endl;   raptor_prompt_variable_zzyz =int;
   cout << raptor_prompt_variable_zzyz << endl;
   cin >> bil;
   while (!(ting(w)==bil))
   {
      cout << "Input Pencrian Salah" << endl;   }
   cout << "Nilai yang dicari di indeks ke- " << endl;
   return 0;
}
